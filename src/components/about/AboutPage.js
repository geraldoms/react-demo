import React, { Component } from "react";
import { Link } from "react-router";

class AboutPage extends Component {
  render() {
    return (
      <div>
        <h1>About</h1>
        <p>This application uses React, React Router and a couple of other libraries</p>
      </div>
    );
  }
}

export default AboutPage;
